#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x;
    string a;
    cin >> x;
    // cin >> a;
    // cin.getline(s, 100);

    cin.ignore();
    getline(cin, a);

    cout << x << endl;
    cout << a << endl;

    return 0;
}
